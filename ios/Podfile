# # Spec repos
# source 'https://github.com/byteplus-sdk/byteplus-specs.git'
# source 'https://github.com/volcengine/volcengine-specs.git'
# source 'https://cdn.cocoapods.org/'

# platform :ios, '13.0'
# ENV['COCOAPODS_DISABLE_STATS'] = 'true'

# project 'Runner', {
#   'Debug' => :debug,
#   'Debug-dev' => :debug,
#   'Debug-prod' => :debug,
#   'Profile' => :release,
#   'Profile-dev' => :release,
#   'Profile-prod' => :release,
#   'Release' => :release,
#   'Release-dev' => :release,
#   'Release-prod' => :release,
# }

# # ---- Robust flutter_root that can work even if Generated.xcconfig isn't created yet
# def flutter_root
#   # Prefer explicit env var if present
#   env_root = ENV['FLUTTER_ROOT']
#   return env_root if env_root && !env_root.strip.empty?

#   # Fallback to Generated.xcconfig (created by `flutter pub get`)
#   generated_xcode_build_settings_path = File.expand_path(
#     File.join('..', 'Flutter', 'Generated.xcconfig'), __FILE__
#   )

#   unless File.exist?(generated_xcode_build_settings_path)
#     raise "#{generated_xcode_build_settings_path} must exist. "\
#           "Run `flutter pub get` from the project root first or set FLUTTER_ROOT env var."
#   end

#   File.foreach(generated_xcode_build_settings_path) do |line|
#     matches = line.match(/FLUTTER_ROOT\=(.*)/)
#     return matches[1].strip if matches
#   end

#   raise "FLUTTER_ROOT not found in #{generated_xcode_build_settings_path}. "\
#         "Try deleting ios/Flutter/Generated.xcconfig, then run `flutter pub get`."
# end

# require File.expand_path(File.join('packages', 'flutter_tools', 'bin', 'podhelper'), flutter_root)

# flutter_ios_podfile_setup

# target 'Runner' do
#   # Prefer static linkage for Flutter pods
#   use_frameworks! :linkage => :static
#   use_modular_headers!

#   # BytePlus / Volcengine pods (require modular headers/module defs)
#   pod 'TTSDKFramework', :modular_headers => true
#   pod 'RangersAppLog', :modular_headers => true

#   flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))

#   target 'RunnerTests' do
#     inherit! :search_paths
#   end
# end

# post_install do |installer|
#   installer.pods_project.targets.each do |target|
#     # Keep Flutter toolchain’s extra flags
#     flutter_additional_ios_build_settings(target)

#     # Ensure module definitions for these pods
#     needs_modular = [
#       'TTSDKFramework',
#       'RangersAppLog'
#       # Add 'byteplus_bd_file_upload' here if/when it has a pod target name
#     ]

#     if needs_modular.include?(target.name)
#       target.build_configurations.each do |config|
#         config.build_settings['DEFINES_MODULE'] = 'YES'
#         config.build_settings['CLANG_ENABLE_MODULES'] = 'YES'
#         # If they rely on non-modular headers:
#         config.build_settings['CLANG_ALLOW_NON_MODULAR_INCLUDES_IN_FRAMEWORK_MODULES'] = 'YES'
#       end
#     end
#   end
# end

# Uncomment this line to define a global platform for your project
platform :ios, '14.0'

# CocoaPods analytics sends network stats synchronously affecting flutter build latency.
ENV['COCOAPODS_DISABLE_STATS'] = 'true'

project 'Runner', {
  'Debug' => :debug,
  'Profile' => :release,
  'Release' => :release,
}

def flutter_root
  generated_xcode_build_settings_path = File.expand_path(File.join('..', 'Flutter', 'Generated.xcconfig'), __FILE__)
  unless File.exist?(generated_xcode_build_settings_path)
    raise "#{generated_xcode_build_settings_path} must exist. If you're running pod install manually, make sure flutter pub get is executed first"
  end

  File.foreach(generated_xcode_build_settings_path) do |line|
    matches = line.match(/FLUTTER_ROOT\=(.*)/)
    return matches[1].strip if matches
  end
  raise "FLUTTER_ROOT not found in #{generated_xcode_build_settings_path}. Try deleting Generated.xcconfig, then run flutter pub get"
end

require File.expand_path(File.join('packages', 'flutter_tools', 'bin', 'podhelper'), flutter_root)

flutter_ios_podfile_setup

target 'Runner' do
  use_frameworks!
  use_modular_headers!

  flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))
  target 'RunnerTests' do
    inherit! :search_paths
  end
end

post_install do |installer|
  installer.pods_project.targets.each do |target|
    flutter_additional_ios_build_settings(target)
    target.build_configurations.each do |config|
      config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '13.0'
     end
  end
end
